version: "3"

services:
  nextcloud:
    image: nextcloud
    restart: always
    env_file:
      - ./nextcloud.env
    volumes:
      - ./nextcloud:/var/www/html
    depends_on:
      - mariadb
    networks:
      - nextcloud_network

  mariadb:
    image: mariadb
    restart: always
    env_file:
      - ./db.env
    volumes:
      - ./mariadb:/var/lib/mysql
    networks:
      - nextcloud_network

  nginx:
    image: nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - nextcloud
    networks:
      - nextcloud_network

networks:
  nextcloud_network:

