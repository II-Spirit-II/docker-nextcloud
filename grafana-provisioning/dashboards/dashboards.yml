apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30 # This can be changed depending on how often you want Grafana to scan for changed dashboards
    options:
      path: /etc/grafana/provisioning/dashboards

